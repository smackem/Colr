<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:lc="clr-namespace:Colr.DesktopApp.Controls">

    <Style TargetType="{x:Type lc:DistributionCurve}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type lc:DistributionCurve}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Image Source="/Resources/hues.png" Stretch="Fill" Opacity="0.7" />
                            <lc:DrawingPane x:Name="PART_CurvePane" />
                        </Grid>
                        <!--<DockPanel Margin="1">
                            <Image Source="/Resources/hues.png" Height="10" Margin="0 1 0 0" DockPanel.Dock="Bottom" Stretch="Fill" />
                            <lc:DrawingPane x:Name="PART_CurvePane" />
                        </DockPanel>-->
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
